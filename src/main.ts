import { duplicateCount } from "./codewars/counting-duplicates";
import { reverseWords } from "./codewars/reverse-words";
import { rps } from "./codewars/rps";
import { merge } from "./leetcode-problems/merge-sorted-array/merge-sorted-array";

merge([1, 2, 3, 0, 0, 0], 3, [2, 5, 6], 3);
merge([1], 1, [], 0);
merge([4, 5, 6, 0, 0, 0], 3, [1, 2, 3], 3);
